# Copy to /etc/infisical/alert-smtp.env (chmod 600). Do not commit real values.
# Used by check-agent.sh to email on Infisical Agent failure.

ALERT_TO=you@example.com
ALERT_FROM=infisical-alerts@win-predict-ai.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
