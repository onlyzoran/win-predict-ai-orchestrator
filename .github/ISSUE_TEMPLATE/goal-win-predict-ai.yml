name: win-predict-ai
description: Цель для семьи win-predict-ai (ui / app / admin / ios / …).
labels: ["win-predict-ai"]
body:
  - type: markdown
    attributes:
      value: |
        Продукт: **win-predict-ai**. Inbox → In Progress — менеджер сам выберет поверхности и откроет PR.
        Правки: комментарий + Review → In Progress. Приёмка: «релизь» + Review → In Progress.
  - type: textarea
    id: outcome
    attributes:
      label: Результат
      description: Что должно быть правдой, когда цель закрыта.
    validations:
      required: true
