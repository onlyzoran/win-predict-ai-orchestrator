name: telegram-bots
description: Цель для telegram-ботов. Рабочие репо пока не подключены.
labels: ["telegram-bots"]
body:
  - type: markdown
    attributes:
      value: |
        Продукт: **telegram-bots** (stub). Репо ещё не подключены — оркестратор не открывает PR.
  - type: textarea
    id: outcome
    attributes:
      label: Результат
      description: Что должно быть правдой, когда цель закрыта.
    validations:
      required: true
