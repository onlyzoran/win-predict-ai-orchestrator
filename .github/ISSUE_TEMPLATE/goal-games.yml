name: games
description: Цель для игр (iOS и др.). Рабочие репо пока не подключены.
labels: ["goal", "product:games"]
body:
  - type: markdown
    attributes:
      value: |
        Продукт: **games** (stub). Репо ещё не подключены — оркестратор не создаёт child.
  - type: textarea
    id: outcome
    attributes:
      label: Результат
      description: Что должно быть правдой, когда цель закрыта.
    validations:
      required: true
