# Personal user API key (Dashboard → API Keys). Not a service-account / team Admin key.
# Same Cursor user as the GitHub secret CURSOR_API_KEY.
CURSOR_API_KEY=cursor_...

# PAT with repo + project on all six win-predict repos. Watch and workers use it as GH_TOKEN.
ORCHESTRATOR_GITHUB_TOKEN=ghp_...

# Optional. Same chat as the GitHub Action secrets.
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
