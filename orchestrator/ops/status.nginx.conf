# Статус оркестратора: http://<ip>/ops/
# include внутри server { listen 80; ... }, лучше до location /

location = /ops {
    return 301 /ops/;
}

location ^~ /ops/ {
    alias /var/www/orchestrator-status/;
    index index.html;
    add_header Cache-Control "no-store";
}
